# 2026-08-10

## 用户问"下一步做什么" + 跨对话连续性确认

- **跨对话连续性**：用户疑问"不管在哪个任务里对话，都能按最新进展做吗"。确认——进展全部落盘（automation memory / changelog / feature-closure-plan / 2.5D 报告），不依赖对话窗口。创建 `MEMORY.md`（项目级长期记忆总纲），任何新对话读此一份即接全局。
- **下一步结论**：当前真正瓶颈是**用户本地 Unity 验证**（本环境无 Unity，且 P0 六项已全交付，再写是库存积压）。
- **交付**：`docs/verification-checklist.md`——供用户本地 Unity 照勾的验证清单（编译 / EditMode / PlayMode / 六键手测 / 视觉 / 决策点）。
- **已知遗留（非阻断）**：P2_01~03 + P4_01~02 共 6 条红；`P0_2_P0_4_PlayModeTests.cs` L73/L377 仍裸 LoadScene 无 isPlaying 守卫（PI11 同类）。
- **验证后分支**：2D 收尾 or 启动 2.5D 技术验证（竹林3D+Spine+砍竹子特效），取决于用户拍板。

## automation 第 8 轮 · P1-3 音效系统全栈落地

- **选题**：P1-3 是 P1 中唯一零代码的项（全仓 `grep AudioSource` 零命中，两个 `PlaySfx` 委托零订阅）。走完整 SOP（PM→架构→工程→QA）。
- **交付**：6 新增 + 1 改 + 1 测试 = 8075 行。`AudioConfig` / `SfxSynth` / `SfxRecipes` / `AudioClipFactory` / `AudioDirector` / `AmbienceLayer` + `CombatBridge` 接线 + 53 条 EditMode 测试。**12 音效 + 1 环境衬底全部程序化合成，零二进制资源**。详见 changelog 阶段 23。
- **关键裁定**：音频吃 `Time.deltaTime`，**永不吃 `FeedbackClock.Delta`**（顿帧期 Delta≡0 → 节流窗口不推进 → 连击第二击被误判重复而哑）。合成用私有固定种子 `System.Random`，绝不碰内核 `SkillRng`/`PCG32`。技能 key 一律引 `SkillConfig.SKILL_*` 常量。
- **护栏方法论沉淀**：静态护栏落地后**必须做变异测试**，否则分不清「没问题」和「没测到」。新增 `Assets/_Project/audio_guard_mutation_test.py`（4/4 变异捕获）；`audio_syntax_check.py` 升级为解析源文件做跨文件双向 key 差集。
- **护栏**：t1 64/64、t3 9/9，2.5294x 未漂移；内核零改动。
- **遗留**：首次 Unity 打开后须提交自动生成的 7 个 `.meta`（否则换机 GUID 错乱）。

## 用户对话 · 竹林 2.5D 技术验证启动

- **背景**：用户问"启动2.5后还能回2.0吗""不知道怎么做""喜欢斩妖行打击感"。用架构隔离图+分支图说明：内核100%复用、回退是零成本 `git checkout main`。打击感=反馈系统，与2D/2.5D无关，2.5D反是加分项。
- **用户拍板**：选「竹林2.5D技术验证」路径（AskUserQuestion q-0）。
- **交付**：
  - 创建 `feature/2.5d` 分支并推送（main 保留 2D 可玩版不动）。
  - `docs/2.5d-tech-verify-plan.md`：3 个验证子项（竹林3D场景/Spine角色/砍竹子特效）+ 验收标准 + 资源清单 + 代码骨架设计 + 里程碑 + 风险回退。
  - `Assets/_Project/Scripts/Runtime/2.5D/IsometricCameraRig.cs`：等距俯视相机骨架（纯Transform，低风险，待真机验证）。
- **下一步（轮次B）**：竹林3D场景+砍竹子特效（验证1.1+1.3），复用战斗内核与 FeedbackClock 顿帧（A-1裁定）。Spine接入留轮次C。
- **回退保障**：不顺眼即 `git checkout main`，本分支零影响主线。
