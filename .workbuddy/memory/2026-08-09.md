# 2026-08-09

## P1-6 玩家成长曲线（自动化第 2 轮，标准 SOP 全流程）

交付：`Progression.cs`(纯逻辑) + 2 个测试文件 + 6 处接线修改 + 4 份设计文档。详见 `docs/changelog.md` 阶段 16。

## 第 7 轮 · 文档修复 + 2.5D 可行性评估（用户问询触发）

- 修复 `docs/unity-t3-prd.md` 闪避资源描述（灵力20→体力25，3处）。
- 产出 `docs/unity-2.5d-feasibility.md`：全项目摸底 + 架构复用度矩阵 + 切换成本 + 三步路线图。
- 核心结论：内核 100% 复用，不推荐现在切换，推荐先完成 2D 再 3 轮技术验证。
- 用户讨论要点：参考《斩妖行》水墨 + LOL 3D场景2D角色 → 八方向 → Spine 骨骼降成本。

**口径**：等级上限 10，`HpMax=260+26×(L-1)`，`AtkBonus=+1×(L-1)`（加法非乘区），1 级加成恒 0，升级不回满，局内成长重开清零。

**本轮最重要的一条工程结论（Q-1）**：
`DifficultyBridge.PlayerHpMax` 语义已正式改为「**平衡基准血量**，永久钉死 260」，**禁止再回写玩家实时血上限**。它是 `d_eff = PlayerHpMax/65` 的分子，是一把标尺；跟着玩家血量涨会导致怪同步变强、玩家白升级、d_eff 从 4.0 漂移。全仓赋值点只允许 2 处：`CombatBridge.cs:444`（常量）与 `CombatController.cs:134`（构建期一次性）。相关注释已推翻重写并写明「别再改回去」。

**踩坑清单（后续开发直接复用）**：
- `Combatant.Level` 是**敌人区域等级**（`DifficultyBridge.cs:328/407` 写入），玩家等级不可复用该字段。
- 玩家有**两条互不引用的 raw 源**：`AttackController.cs:35 AttackRaw`（普攻，始终生效）与 `SkillConfig.cs:190 BASIC_RAW`（技能表）。改攻击力必须两侧都改，否则"升级后普攻毫无变化"。
- ~~`t3_selfcheck.py` 的 `T3_PURE` 与 `EXPECTED_COUNT` 是写死清单，新增纯逻辑文件必须登记~~ **（修订：见 P1-2 条目——此条有误，新增文件靠 L700 `rglob("*.cs")` 自动纳入，无需登记）**。
- `Combatant.Hp`/`HpMax` 是**透传到 `WCore.cs:65/68` 的裸字段，无钳制**（此前架构文档误称有钳制，已订正）。
- 浮点边界测试要按 **ULP** 挑值：float32 在 [32,64) 的 ULP≈3.81e-6，`34.999999f` 会被舍入成精确 `35.0f`。
- **假绿高发区**：两侧传同一来源的等值断言恒真。写法上要先断言"基准值确实 != 对照值"再比对。

**护栏基线（主理人复跑）**：t3 **9/9**；t1 **64/64**，倍率 **2.5294x** 未动。

**诚实边界**：本环境无 Unity/dotnet，未编译未跑 NUnit。

## BugFix 复修：Test Runner 7 红 + 占位符清理（自动化第 2.5 轮，BugFix 路径）

用户实测 Test Runner **192 通过 / 7 失败**（P2_01~03 + P4_01~02 + MENU09 + PI11），授权"随便改不请求"。复修 3 个测试文件，**零生产代码改动**。

**根因分流**：
- MENU09 红 = 纯测试隔离污染（`MainMenuHud.SkipOnNextLoad` 是 static、跨夹具存活，上游 PlayMode 未干净复位）。→ 改探针语义，如实报红而非假绿。
- P2/P4 红 = QA 抓到 2 个真 bug：① 帧等待(`yield return null`)≠内核步进（`CombatScheduler.Tick` 固定步长累加，快机 2 帧可能跑 0 逻辑步）；② `WaitForEnemies` 不消费帧直接返回，清场发生在 `RunPhase._armed` 武装前，Won 永不触发。→ 新增 `WaitForKernelSteps`(锚 `Scheduler.TotalSteps`)/`WaitForRunOver`(轮询 `Phase`) 轮询。
- PI11 红 = 待本地 Console 定位（已加诊断消息读 `actualTarget`）。

**占位符澄清（重要）**：`SampleScene` **已是真实战斗场景**（GameObject 1602164645 同时挂 CombatBridge+CombatController，zoneId=zone_youhuang，playerHpMax=260）。`GameplaySceneName="SampleScene"` 本就正确，**只清掉了误导性的 `TODO(用户)` 注释**，没有也不该改场景名。

**护栏（主理人复跑 2 次）**：t3 **9/9**；t1 **64/64**，倍率 **2.5294x** 未动。`TODO(用户)` 三文件 grep 零命中。

**遗留给用户**：本地 Unity 重跑 Test Runner；PI11 仍红时把完整 Console 报错发回精修根因。

## P1-2 受击反馈（自动化第 3 轮，标准 SOP 全流程；本轮回为收尾落盘）

实现在上一自动轮完成，本轮回补齐文档收尾：changelog 阶段 18 + 跨仓 daily log + 自动化 memory。详见 `docs/changelog.md` 阶段 18 与 `docs/unity-p1-2-hitfeedback-*`、`docs/p1-2-qa-report.md`。

**核心裁定**：顿帧**禁用 Time.timeScale**（会冻确定性内核、破坏 2.5294x 指纹）→ 三层时钟分离，新增 `FeedbackClock`（`static Frozen` + `Delta => Frozen?0:Time.deltaTime`）；所有表现层计时改吃 `FeedbackClock.Delta`。

**玩家受击闪白缺口（R-03 原空操作，工程师发现）**：`CombatController.AttachView()` 只对敌人注册 `CombatView`，玩家从未注册 → 新建只染色的 `PlayerHitFlash`（仅碰 `SpriteRenderer.color`，绝不碰 transform，避免与 PlayerController 抢写 position 抽搐）。

**QA 真缺陷（已修源码）**：P1-01（`HitFeedbackDirector.OnHitFeedback/OnEnemyDied` 禁用态仍改 `FeedbackClock.Frozen` → `:336`/`:397` 加 `isActiveAndEnabled` 守卫）；P1-02（`HitFeedbackConfig` 的 static 无复位钩子 → `ResetStatics()` + `[RuntimeInitializeOnLoadMethod(SubsystemRegistration)]`）。QA 故意留 1 条指认 P1-01 的用例，工程师修源码后未篡改测试。

**护栏（主理人独立复跑）**：t3 **9/9**（83 .cs → 类型 146/命名空间 17，全可解析）；t1 **64/64**，倍率 **2.5294x** 未动。

**★ 订正（重要）**：第 2 轮踩坑清单"t3 的 `T3_PURE`+`EXPECTED_COUNT` 是写死清单、新增纯逻辑文件必须登记否则红"**有误**。实测 `T3_ALL=T3_PURE+T3_UNITY+T3_BRIDGE` 是自洽 33 文件集（`len(T3_ALL)==EXPECTED_COUNT` 恒真，与新增无关）；新文件靠 **L700 `rglob("*.cs")` 全树纳入类型解析**。故新增文件**无需登记**；若强行登记须同步 +1 EXPECTED_COUNT，否则 A1 红。第 2 轮给 Progression.cs 登记的改动属多余（无害）。

**遗留给用户**：本地 Unity 编译 + Test Runner 跑 `P1_2_HitFeedbackTests`（43 条，P1-01 修复后应全绿）+ 目视验顿帧/屏震/分阵营闪白/伤害飘字；`HitFeedbackConfig` 强度系数（如 `HeavyRatioEnemy=0.45f`）为起调值，待实测普攻伤害后校准。

## 用户问询轮 · 操作屏/伤害飘字快速补强 + PI11 EditMode LoadScene 根治（08-09 晚间）

用户实测 Test Runner **193 通过 / 6 失败**（MENU09 已绿，P2_01~03 + P4_01~02 仍红），PI11 Console 报错明确为 EditMode 下调 `SceneManager.LoadScene` 抛 `InvalidOperationException`（需 `EditorSceneManager.OpenScene`）。用户授权"先把操作屏补上，最起码能看到伤害"。

**走快速模式**（团队 `software-combat-hud-damage-numbers-f1a6`）。关键教训：**首派工程师遭遇网络 502（代理/防火墙截断 HTTPS）失败，但崩溃前已把 (a) 整套飘字系统写盘、(b) PI11 修成完整 `Application.isPlaying` 分支**——其实飘字系统来自更早的 P1-2 SOP 轮已存在实现，只是未充分验证/用户未看到。二次重派工程师复核确认前任已完成，**纯验证 IS_PASS: YES（0 文件改动）**。

**交付（已落盘、未 git 提交）**：
- `DamagePopupLayer.cs` / `HitFeedbackDirector.cs` / `HitFeedbackConfig.cs` / `PlayerHitFlash.cs`（表现层，Xianxia.Unity.T2）
- `CombatBridge.cs` 新增 `SetupHitFeedback()`（L344 调 / L500 定义），订阅 `CombatEventsUnity.HitFeedback`/`EnemyDied`
- `P1_6_ProgressionIntegrationTests.cs` PI11 修复为 isPlaying 分支，EditMode→`EditorSceneManager.OpenScene`+手动驱动最小生命周期链，原 `260` 三处断言保真

**QA 严过关 · 路由 NoOne（通过）**：t3 **9/9**、t1 **64/64 @ 2.5294x**；独立核实 126 处运行时 + 132 处测试静态符号引用**零断链**，程序集引用 T2→Unity 单向合法；PI11 手动链四 `SendMessage` 目标全真实；环形池/单一分档/对称接线逻辑全过。新增 `tools/qa_p12_linkcheck.py`、`tools/qa_p12_balance.py` 补 t3 D2/B 覆盖不到的 T2 树盲区。

**遗留（非阻断）**：① `DamagePopupLayer` 双 `<summary>` 错位（CS1571 警告）；② `P1_6...Tests.cs:486` 裸 `FindObjectOfType` 未加 `#if UNITY_2023_1_OR_NEWER` 守卫（CS0618 告警）；③ **存量** `P0_2_P0_4_PlayModeTests.cs` L73/L377 仍裸 `SceneManager.LoadScene` 无 isPlaying 守卫，与 PI11 bug 同类，建议独立跟进；④ 无 Unity 环境，真机行为（SendMessage Awake 拉起 EventsUnity、视觉手感）只能本地验证。

**诚实边界**：本环境无 Unity/dotnet，全部改动未经编译、未跑 NUnit。

## BugFix · P1-2 飘字合并窗口修复（自动化第 5 轮，BugFix 快捷路径）

用户实测 Test Runner **222 passed / 20 failed**（PI11 已绿，但 `P1_2_HitFeedbackTests` 大量 `Popup_*` 红，核心 `Popup_SameTargetWithinWindow_MergesIntoOneSlot` 报 `Expected:1 But:0`）。

**根因**：`DamagePopupLayer.TryPlace` 投影不可用（`Camera.main==null`/`_canvasRect==null`/`TryProject` 失败）时静默 `return false` → 整条 `Push` 丢弃 → 两次命中都没建槽 → 计数恒 0。仅 EditMode 测试暴露，真实战斗投影走通不触发。

**修复（1 文件、最小改动）**：`TryPlace`(L710-719) 改为投影失败→降级画布中心落点 + `return true`。"命中必有回应"优先于精确位置；真实手感零变化。

**★ QA 关键澄清（重要，避免误判）**：P1-01 用例 `Frozen_StaysReleased_WhenDisabledDirectorStillReceivesEvent` 被误列为"预期红"，但 `HitFeedbackDirector.cs:336/397` 的 `isActiveAndEnabled` 守卫**第 3 轮已落地**，该用例现在应转绿。→ `P1_2_HitFeedbackTests.cs:9-12` 文件头"有 1 条预期失败"注释**已过期**，建议下轮顺手清（本轮未动文件）。

**QA 严过关 · 路由 NoOne（通过）**：t3 **9/9**、t1 **64/64 @ 2.5294x**；静态断链零（含 CS0165 专项论证）；逻辑对拍模型 `tools/qa_popup_fallback_model.py` **34/34 PASS**（证明投影不可用⇒降级⇒断言满足，真机路径逐位一致）。

**遗留（非阻断）**：① R3 真机进场首帧 `Camera.main` 未装配时新行为在画布正中弹未挂靠敌人飘字（UX 取舍，本地留意）；② 存量 `P0_2_P0_4_PlayModeTests.cs` L73/L377 仍裸 `SceneManager.LoadScene` 无 isPlaying 守卫（PI11 同类），仍建议独立跟进。

**诚实边界**：本环境无 Unity/dotnet，未编译未跑 NUnit，结论建立在静态审查 + 逻辑对拍模型上。

## 三项收尾（用户授权"1、2、3 全做"）：P0_2_P0_4 PlayMode 红 + 美术特效框架 Round 2 + 像素动画排查（08-09 傍晚，团队 software-combat-fx-framework）

用户授权主理人自主推进 1=清测试红、2=美术特效、3=像素动画，顺序由我定，不反复请求。详见 changelog 阶段 21。

**① P0_2_P0_4 5 条红（双根因 + asmdef 迁移，任务 3 完成）**：
- 根因 A：`Xianxia.Unity.T2.Tests` asmdef `includePlatforms:["Editor"]` → 5 条实际以 EditMode 跑，首句 `LoadScene` 抛 InvalidOperationException 必红。→ `LoadGameplayScene` 首句 `if (!Application.isPlaying) { Assert.Ignore; yield break; }`（置于 SkipOnNextLoad=true 之前防 static 污染 MENU09）。
- 根因 B：P0-6 引导面板冻结调度器（无输入永不关）。→ 新增 `BeginCombat(bridge)` helper：`guide.HidePanel()` 弱化真人路径解冻，找不到降级 `bridge.SetMenuPaused(false)`。
- **关键落地：asmdef 迁移** → `P0_2_P0_4_PlayModeTests.cs` 迁 `Tests/PlayMode/` + 新 `Xianxia.Unity.T2.PlayModeTests.asmdef`（无 includePlatforms）→ 从"EditMode Ignore"变"PlayMode 真执行"。迁移前确认全树引用均为注释文本无代码断链。
- 教训：**asmdef includePlatforms 会把"PlayMode 测试"锁死在 EditMode**，此类"首句 LoadScene 必红"先用 asmdef 归属排查，别只盯着等待逻辑。

**② 美术特效框架 Round 2（QA 双轮，路由 NoOne 放行，任务 1 完成）**：
- 上轮 QA 抓 4 缺陷，工程师修复（429 断连前已写盘，主理人实盘核实 3 文件括号全平 + 常量类型已改）→ QA Round 2 独立复核通过。
- BUG-1 CS1513 缺 `}` → 补平 82/82；BUG-2/3 CS0266 `HitFxCapacity/Min/Max` const float→**const int**(24/1/128)（全树仅 3 调用点均 int 语义）；BUG-4 第五路冻结不对称 → 正常态补 `_hitFx.FreezeAll(false)`(L270-273) 与 menuPaused `FreezeAll(true)`(L936) 对称。
- 护栏：t3 9/9、t1 64/64 @ 2.5294x 未动（主理人 + QA 各复跑）。

**③ 像素动画（任务 2 完成，代码层无解）**：
- 排查 HeroineAnimator/HeroineFrames/SpriteFactory 全链路无代码 bug；**根因 = 精灵内容**（主理人逐张视觉核验坐实）：walk_side/up 6 帧"站桩换皮"（"一拐一拐"）、walk_down 第4帧下沉（"站起来趴下"）、attack 6 帧只画剑缺身体（攻击隐身）、dodge 帧漂移。idle/hurt/death OK。
- 交付 = 精确美术补图清单（文件/帧号/要求/尺寸/命名/PPU/pivot 契约，见工程师报告 §3）；零代码改动。
- 代码次级隐患（朝向判定无 hysteresis）评估边际收益主动不动。
- **给美术的硬契约**：路径 `Assets/StreamingAssets/characters/heroine/`、`<prefix>_<N>.png` 从 1 连续、PPU=1（画布像素=世界单位）、背景透明、walk 脚底基线距底边 ≤18px（FootPivot y=0.28）、attack 身体在画布左半（AttackPivot x=0.25）。

**护栏（主理人复跑）**：t3 **9/9**、t1 **64/64 @ 2.5294x** 未动。本轮改动无 git 提交。
**遗留给用户**：本地 Unity 编译 + Test Runner（P0_2_P0_4 5 条 PlayMode 真绿确认）+ 粒子特效挂 prefab 目视 + 动画按清单补图。
