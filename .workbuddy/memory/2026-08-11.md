# 2026-08-11 工作日志

## 第 11 轮（自动化触发，用户不在场）· 分叉风险收束 + BossPending 内核护栏

**选题理由**：库存积压警告已连续两轮（P0 全交付 + P1 交付 4/7 + P2-1 已接线，但全部"待用户本地验证"且从未被验证过）。本轮**刻意不新增待验证的 Unity 表现层模块**，改为收束两个能在无 Unity 环境下自证的真实风险。

**工作流**：📋 部分工作流（架构评审 + QA 护栏），架构师与 QA 双线并行派工。

### 交付

| 文件 | 行数 | 说明 |
|---|---|---|
| `docs/branch-merge-plan.md` | 783 | 分支合并协调方案（2 Mermaid + 259 行表格） |
| `Assets/Scripts/Systems/Combat/Tests/bosspending_selfcheck.py` | 1022 | 主护栏 53/53 |
| `Assets/Scripts/Systems/Combat/Tests/bosspending_guard_mutation_test.py` | 565 | 变异测试 26/26，捕获率 18/18 |
| `docs/p2-1-bosspending-qa-report.md` | 349 | QA 报告 + 诚实边界 |
| `docs/changelog.md` | +阶段 26 | 含编号裁定与雷排除 |

C# 生产代码**零改动**（modified 列表与派工前完全一致）。

### 关键发现（跨轮次复用价值高）

1. **已提交部分零冲突**：`git merge-tree --write-tree main feature/2.5d` 退出码 0。**全部冲突 100% 来自未提交工作树**（约 5416 行无 git 备份）→ 合并第一步必须是落盘 commit。
2. **CRLF 预演陷阱**：仓库 `core.autocrlf=true`（blob 纯 LF / 工作树纯 CRLF）。拿 `git show` 导出的文件直接与工作树文件三方合并 → **整文件冲突假象**。手工比对前必须归一化行尾。真实 `git merge` 不受影响。
3. **`CombatBridge.cs` 真冲突只有 1 处**（`-1366` 同锚点）。`-343`/`-357` 间隔 9 行不重叠，git 自动合并。
4. **`Bootstrap.cs` 确认严格超集**（`diff main→feat` 删除行数 = 0）。合并**禁用 `-X ours/theirs`**：误用会静默丢 `ResetStatics`+幂等订阅 → 黑屏复发，且本环境四项自证全都发现不了（纯 PlayMode 行为，只在第 2 次进 Play 暴露）。
5. **变异测试抓到 QA 自己的护栏漏洞**：M-B3（失败优先改成胜利优先）首轮未捕获——原用例自带 BOSS 债，`PendingAware = 0+1 = 1` 使判胜分支 `count <= 0` 本就不成立，**债自己把判胜分支挡死了**，对调 ④⑤ 照样 Lost。**判别性用例必须无债**。已拆为 BP-B2a/B2b 后捕获。再次印证：护栏落地必做变异测试。
6. **`.py` 护栏全部不入库**：`.gitignore:44` 排除 `Assets/Scripts/**/Tests/*.py`，`git ls-files "*.py"` 返回空。t1/t3/bosspending 四个脚本**clone 即丢**。口径还不统一（main 的 `Assets/_Project/audio_syntax_check.py` 是入库的）。待用户拍板是否开白名单。

### 主理人裁定（3 条）

- **D-1**：R-4 硬超时（12s）销债后 `_armed` 已亮 → 直接 Won，玩家没打到 BOSS 却弹胜利。**接受现状**（软锁远比误判胜利严重），登记待办要求留诊断痕迹。
- **D-2**：**不**让内核护栏伸手进 `_Project/Scripts/Runtime/` 钉 I-3。理由：2.5D 会重写表现层，内核护栏一旦引用表现层，重写时三套护栏集体变红，无法区分"内核坏了"与"表现层换了"。I-3 由 `P2_1_BossWiringTests.cs` 负责。
- **D-3**：不变量统一按 **4 条**表述（简报漏了 I-4「Clear() ⟹ 三字段归零」，`Encounter.cs:188`）。已把 D9 检查由钉 3 条收紧为钉 4 条，复跑仍 53/53。

### changelog 阶段编号雷（第 10 轮埋下，本轮排除）

第 10 轮只看到 main 占了阶段 23，**漏看 feature 自己也有一个阶段 23**（P2-1 接线，08-09 夜）→ 合并后两个「阶段 23」且 git 报 `rc=0` 无冲突标记（**唯一"工具报绿、结果是错的"处**）。

**裁定**：阶段号按**落盘时序**递增（一贯口径）。终态 22 → 23(P2-1 接线) → 24(P1-3 音效，**合并时 main 侧 23→24**) → 25(第10轮) → 26(本轮)。feature 侧已执行完毕（`##`→`###` 层级也统一），第 10 轮误导性提示已划删除线注明作废。

### 护栏（主理人亲自复跑）

`t1` **64/64 @ 2.5294x 未漂**；`t3` **9/9**（90 .cs / 类型 155 / 命名空间 17）；`bosspending_selfcheck` **53/53**；变异测试 **26/26**。`Encounter.cs`/`RunPhase.cs` SHA-256 跑前跑后逐字节一致。

### 遗留给用户

1. 按 `docs/branch-merge-plan.md` §8 执行合并，**第一步是落盘 commit**（5416 行无备份是全场唯一单点风险）。
2. 合并时 `CombatBridge.cs` `-1366` 人工裁决；`Bootstrap.cs` 自动合并但禁 `-X ours/theirs`。
3. 合并后 main 侧「阶段 23 · P1-3」改 24 并移位。
4. 本地 Unity 编译 + Test Runner（RP15–RP19 / P0_4 9 条 / P2_1 / P1_3 53 条）。
5. 拍板 `.py` 护栏是否开 gitignore 白名单。
6. 待办 D-1（R-4 超时判胜的诊断痕迹）。

**诚实边界**：无 Unity / 无 dotnet。护栏证明的是「Python 参考模型自洽」+「C# 源文本锚点与模型一致」，**不等于编译通过、不等于 NUnit 通过**。
